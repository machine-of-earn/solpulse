"""solpulse — a zero-dependency Solana ecosystem state reporter.

Standard library only. No API keys, no pip installs. Every data source either
returns real data or reports an explicit error; nothing is ever estimated or
silently defaulted.
"""

__version__ = "0.1.0"
